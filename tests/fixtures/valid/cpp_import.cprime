import "imports/native_math.cpp";

function main() {
    NativeDistance distance = {
        value = native_seed
    };
    primitive uint32 value = native_add(distance.value, 5);
    println(value);
}